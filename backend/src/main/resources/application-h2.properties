# ===================================================================
# H2 Database Configuration (Mocking Oracle)
# 啟動方式: java -jar app.jar --spring.profiles.active=h2
# ===================================================================

# 使用 Oracle 相容模式
spring.datasource.url=jdbc:h2:mem:testdb;MODE=Oracle;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# 初始化 Schema (因為記憶體資料庫是空的，需要自動建表)
# 請將建表語法放在 src/main/resources/schema.sql
spring.sql.init.mode=always
spring.sql.init.platform=h2

# 讓 H2 Console 可用 (http://localhost:8080/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# 關閉 Jasypt (開發環境通常不需要加密密碼，或者這裡沒用到加密字串)
spring.config.import=ignore:classpath:application-secret.properties

# ���w��l�Ƹ�Ƹ}�� (�u�b H2 ���Ұ���)
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql
