# =============================================================================
# 敏感設定範本 (application-secret.properties.example)
# =============================================================================
# 請複製此檔案為 application-secret.properties 並填入實際值
# 此檔案 (application-secret.properties) 已被 .gitignore 排除，不會被提交
# =============================================================================

# Oracle Database 連線設定
spring.datasource.url=jdbc:oracle:thin:@//your-host:1521/your-service
spring.datasource.username=your_username
spring.datasource.password=your_password

# 如果使用 Jasypt 加密，密碼格式為: ENC(加密後的字串)
# 啟動時需傳入金鑰: java -jar app.jar --jasypt.encryptor.password=YOUR_ENCRYPTION_KEY
# spring.datasource.password=ENC(加密後的密碼)
